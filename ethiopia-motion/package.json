{
  "name": "ethiopia-motion",
  "version": "1.0.0",
  "private": true,
  "description": "Africa map motion graphic highlighting Ethiopia, rendered to MP4",
  "scripts": {
    "build:map": "node scripts/build-map.js",
    "preview": "node scripts/render.js --preview",
    "render": "node scripts/render.js",
    "all": "npm run build:map && npm run render"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "ffmpeg-static": "^5.3.0",
    "gsap": "^3.15.0",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
